source 'https://rubygems.org'

gemspec

# Currently, v1.0.18 on RubyGems does not have the bug fixes for default inclusion of entities functionality. Hence, source the gem directly from Github for the moment.
gem 'typogruby', :git => 'https://github.com/avdgaag/typogruby.git'